when we used Strategy Design Pattern??

when we have two or more child class overriding same function with same logic and that code is not present in the Base code
(parent class) then we will used Strategy Design pattern